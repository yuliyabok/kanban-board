<!-- Справка по shared contracts: общий язык JSON/API между приложением и сервером. -->

# Kanban Contracts

Shared wire contracts for the Flutter app and Dart server.

This package owns route constants, API error shape, sync/realtime contracts,
task history entries, and the "what changed while you were away" summary DTO.
Client domain entities and Drift tables stay inside the Flutter app.
